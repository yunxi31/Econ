<UserControl x:Class="MotorTestSystem.Views.DataPersistenceMonitorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="400">
    
    <UserControl.Resources>
        <Style TargetType="Border" x:Key="MetricCardStyle">
            <Setter Property="Background" Value="#212431"/>
            <Setter Property="BorderBrush" Value="#333645"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource MetricCardStyle}">
        <StackPanel Orientation="Horizontal">
            <!-- 写入通道占用率 -->
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="通道占用率" Foreground="#8A8D99" FontSize="11"/>
                <TextBlock Text="{Binding WriteChannelUtilization, StringFormat={}{0:P1}}" 
                           FontSize="18" FontWeight="Bold"
                           Foreground="{Binding ChannelUtilizationColor}"/>
            </StackPanel>

            <!-- 死信队列数 -->
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="死信队列" Foreground="#8A8D99" FontSize="11"/>
                <TextBlock Text="{Binding DeadLetterCount}" 
                           FontSize="18" FontWeight="Bold"
                           Foreground="{Binding DeadLetterCountColor}"/>
            </StackPanel>

            <!-- 丢弃计数 -->
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="丢弃计数" Foreground="#8A8D99" FontSize="11"/>
                <TextBlock Text="{Binding WriteDroppedCount}" 
                           FontSize="18" FontWeight="Bold"
                           Foreground="#8A8D99"/>
            </StackPanel>

            <!-- 数据丢失告警指示器 -->
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="数据丢失" Foreground="#8A8D99" FontSize="11"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Margin="0,0,4,0"
                             Fill="{Binding DataLossAlertColor}"/>
                    <TextBlock Text="{Binding DataLossCount, StringFormat={}丢失{0}条}" 
                               FontSize="14" FontWeight="Bold"
                               Foreground="{Binding DataLossAlertColor}"/>
                </StackPanel>
            </StackPanel>

            <!-- 手动补传按钮 -->
            <Button Content="补传" 
                    Command="{Binding RetryDeadLetterCommand}"
                    Width="60" Height="26"
                    Background="#333645" Foreground="#00D2E6"
                    BorderThickness="1" BorderBrush="#00D2E6"
                    FontSize="12" FontWeight="Bold"
                    VerticalAlignment="Center"
                    ToolTip="手动触发死信队列补传"/>
        </StackPanel>
    </Border>
</UserControl>
