<UserControl x:Class="MotorTestSystem.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MotorTestSystem.Views"
             xmlns:vm="clr-namespace:MotorTestSystem.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="760" d:DesignWidth="1100"
             Background="{DynamicResource BgBrush}">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#090E12"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B242E"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#242E3A"/>
        <SolidColorBrush x:Key="LineBrush" Color="#344252"/>
        <SolidColorBrush x:Key="CyanBrush" Color="#10DDF7"/>
        <SolidColorBrush x:Key="GreenBrush" Color="#00E68A"/>
        <SolidColorBrush x:Key="RedBrush" Color="#FF5258"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#A7B1BA"/>
        <SolidColorBrush x:Key="DimBrush" Color="#78838C"/>

        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>

        <!-- Search TextBox Style -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#090E12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#344252"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6,8,6"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="0" Data="M15.5,14 L14.3,14 L13.9,13.6 C15.4,11.9 16,9.6 15.6,7.2 C15,4 12.3,1.4 9,1 C4.9,0.5 1.5,3.9 2,8 C2.4,11.3 5,14 8.2,14.6 C10.6,15 12.9,14.4 14.6,12.9 L15,13.3 L15,14.5 L22,21.5 L24,19.5 L17,12.5 Z M9,12 A4,4 0 1,1 9,4 A4,4 0 0,1 9,12 Z" 
                                      Fill="#78838C" Width="14" Height="14" Margin="10,0,5,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                <ScrollViewer Grid.Column="1" x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                <TextBlock Grid.Column="1" x:Name="watermark" Text="搜索操作员账号或姓名..." Foreground="#5A5E73" FontSize="12" Margin="7,0,5,0" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="8,6,8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#242E3A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CyanBrush}"/>
                                <Setter Property="Foreground" Value="#090E12"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dark ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1B242E"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#344252"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,3,32,3"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MinWidth="24" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" Placement="Bottom" Margin="1" VerticalOffset="4" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border x:Name="dropDownBorder" Background="#1B242E" BorderBrush="#344252" BorderThickness="1" CornerRadius="4" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                            <ToggleButton x:Name="toggleButton" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" SnapsToDevicePixels="true">
                                            <Border x:Name="splitBorder" BorderBrush="Transparent" BorderThickness="1" HorizontalAlignment="Right" Margin="0" SnapsToDevicePixels="true" Width="24">
                                                <Path x:Name="arrow" Data="M0,0 L3.5,4 L7,0 Z" Fill="#8A8D99" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center"/>
                                            </Border>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="templateRoot" Property="Background" Value="#242E3A"/>
                                                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                                <Setter TargetName="arrow" Property="Fill" Value="#FFFFFF"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="true">
                                                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource CyanBrush}"/>
                                                <Setter TargetName="arrow" Property="Fill" Value="{StaticResource CyanBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Hyperlink Button Style -->
        <Style x:Key="HyperlinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="26">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="26"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL: USER LIST -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ROW 0: HEADER -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="用户管理" Foreground="White" FontSize="22" FontWeight="Bold"/>
                    <TextBlock Text="管理系统访问权限与操作员信息" Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Add User Button -->
                <Button Grid.Column="1" Command="{Binding AddUserCommand}" Height="36" Width="110" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource CyanBrush}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Path Data="M4,10 L12,10 M8,6 L8,14 M17,10 A4,4 0 1,0 17,2 A4,4 0 1,0 17,10 M11,20 C11,17 15,15 17,15 C19,15 23,17 23,20 L23,21 L11,21 Z" 
                                                      Stroke="Black" StrokeThickness="1.8" StrokeLineJoin="Round" Width="15" Height="15" Margin="0,0,6,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                                <TextBlock Text="新增用户" Foreground="Black" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#33E5FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- ROW 1: SEARCH & FILTER BAR -->
            <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}" Margin="0,18,0,18" Padding="15,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="48"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Input -->
                    <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchTextBoxStyle}"/>

                    <!-- Role Filter Dropdown -->
                    <ComboBox Grid.Column="2" 
                              ItemsSource="{Binding RoleFilters}" 
                              SelectedItem="{Binding SelectedRoleFilter}" 
                              Style="{StaticResource DarkComboBoxStyle}"/>
                </Grid>
            </Border>

            <!-- ROW 2: USER DATA TABLE -->
            <Border Grid.Row="2" Style="{StaticResource PanelBorderStyle}" Padding="10">
                <DataGrid ItemsSource="{Binding Users}"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          Background="Transparent" 
                          BorderThickness="0" 
                          RowBackground="Transparent" 
                          Foreground="White"
                          FontSize="14"
                          GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#252E3A" 
                          HeadersVisibility="Column" 
                          RowHeaderWidth="0" 
                          IsReadOnly="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          ScrollViewer.CanContentScroll="True">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#242E3A" />
                            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Height" Value="48" />
                            <Setter Property="Padding" Value="14,0,14,0" />
                            <Setter Property="BorderThickness" Value="0,0,0,1.2" />
                            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Height" Value="58" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#242E3A" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#2D3A4A" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="14,0,14,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <!-- Account Column -->
                        <DataGridTextColumn Header="账号" Binding="{Binding Account}" Width="130"/>

                        <!-- Name Column with ID Icon -->
                        <DataGridTemplateColumn Header="姓名" Width="170">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <!-- User Icon -->
                                        <Path Data="M12,12 A3,3 0 1,0 12,6 A3,3 0 1,0 12,12 M12,14 C9,14 4,16 4,19 L4,21 L20,21 L20,19 C20,16 15,14 12,14" 
                                              Fill="{StaticResource CyanBrush}" Width="16" Height="16" Margin="0,0,9,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" Foreground="White" VerticalAlignment="Center" FontSize="14"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Role Column -->
                        <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="110"/>

                        <!-- Status Column -->
                        <DataGridTemplateColumn Header="状态" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border x:Name="StatusBorder" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" BorderThickness="1.2" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Ellipse x:Name="StatusDot" Width="7" Height="7" Margin="0,0,6,0"/>
                                            <TextBlock x:Name="StatusText" Text="{Binding Status}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="在线">
                                            <Setter TargetName="StatusBorder" Property="Background" Value="#1A00E68A"/>
                                            <Setter TargetName="StatusBorder" Property="BorderBrush" Value="#4000E68A"/>
                                            <Setter TargetName="StatusDot" Property="Fill" Value="#00E68A"/>
                                            <Setter TargetName="StatusText" Property="Foreground" Value="#00E68A"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="离线">
                                            <Setter TargetName="StatusBorder" Property="Background" Value="#1A78838C"/>
                                            <Setter TargetName="StatusBorder" Property="BorderBrush" Value="#4078838C"/>
                                            <Setter TargetName="StatusDot" Property="Fill" Value="#A7B1BA"/>
                                            <Setter TargetName="StatusText" Property="Foreground" Value="#A7B1BA"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="禁用">
                                            <Setter TargetName="StatusBorder" Property="Background" Value="#1AFF5258"/>
                                            <Setter TargetName="StatusBorder" Property="BorderBrush" Value="#40FF5258"/>
                                            <Setter TargetName="StatusDot" Property="Fill" Value="#FF5258"/>
                                            <Setter TargetName="StatusText" Property="Foreground" Value="#FF5258"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Last Login Time Column -->
                        <DataGridTextColumn Header="最后登录时间" Binding="{Binding LastLoginTime}" Width="185"/>

                        <!-- Actions Column -->
                        <DataGridTemplateColumn Header="操作" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="编辑" 
                                                Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}" 
                                                Style="{StaticResource HyperlinkButtonStyle}"
                                                Foreground="{StaticResource CyanBrush}" 
                                                Margin="0,0,16,0"/>
                                        <Button Content="重置密码" 
                                                Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                CommandParameter="{Binding}" 
                                                Style="{StaticResource HyperlinkButtonStyle}"
                                                Foreground="{StaticResource CyanBrush}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- RIGHT PANEL: ROLE PERMISSION CONFIGURATION -->
        <Border Grid.Column="2" Style="{StaticResource PanelBorderStyle}" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <!-- Shield Icon -->
                    <Path Data="M12,2 L2,6 L2,12 C2,18.5 6.3,21.2 12,22 C17.7,21.2 22,18.5 22,12 L22,6 L12,2 Z" 
                          Stroke="{StaticResource CyanBrush}" StrokeThickness="2" StrokeLineJoin="Round" Width="18" Height="18" Stretch="Uniform" VerticalAlignment="Center"/>
                    <TextBlock Text="角色权限配置" Foreground="White" FontSize="16" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Roles List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Role 1: Administrator -->
                        <Border Background="{StaticResource PanelAltBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6" Margin="0,12,0,0" Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="管理员" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" Content="修改" Style="{StaticResource HyperlinkButtonStyle}" Foreground="{StaticResource CyanBrush}" FontSize="13"/>
                                </Grid>

                                <WrapPanel Grid.Row="1" Margin="0,10,0,0">
                                    <!-- Highlighted Tag -->
                                    <Border Background="#1A10DDF7" BorderBrush="{StaticResource CyanBrush}" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="系统权限" Foreground="{StaticResource CyanBrush}" FontSize="12" FontWeight="SemiBold"/>
                                    </Border>
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="参数修改" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="用户管理" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </Border>

                        <!-- Role 2: Operator -->
                        <Border Background="{StaticResource PanelAltBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6" Margin="0,15,0,0" Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="操作员" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" Content="修改" Style="{StaticResource HyperlinkButtonStyle}" Foreground="{StaticResource CyanBrush}" FontSize="13"/>
                                </Grid>

                                <WrapPanel Grid.Row="1" Margin="0,10,0,0">
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="设备控制" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="查看看板" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </Border>

                        <!-- Role 3: Maintainer -->
                        <Border Background="{StaticResource PanelAltBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="6" Margin="0,15,0,0" Padding="15">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="维护员" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" Content="修改" Style="{StaticResource HyperlinkButtonStyle}" Foreground="{StaticResource CyanBrush}" FontSize="13"/>
                                </Grid>

                                <WrapPanel Grid.Row="1" Margin="0,10,0,0">
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="诊断功能" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="设备校准" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="数据查看" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                    <Border Background="#181D26" BorderBrush="#2A3542" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,8,8">
                                        <TextBlock Text="报警管理" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
                                    </Border>
                                </WrapPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
