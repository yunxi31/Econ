<UserControl x:Class="MotorTestSystem.Views.MonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:root="clr-namespace:MotorTestSystem"
             mc:Ignorable="d"
             d:DesignHeight="980"
             d:DesignWidth="1350"
             FontFamily="Microsoft YaHei UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="BgBrush" Color="#090E12"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B242E"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#242E3A"/>
        <SolidColorBrush x:Key="LineBrush" Color="#344252"/>
        <SolidColorBrush x:Key="CyanBrush" Color="#10DDF7"/>
        <SolidColorBrush x:Key="GreenBrush" Color="#00E68A"/>
        <SolidColorBrush x:Key="RedBrush" Color="#FF5258"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#A7B1BA"/>
        <SolidColorBrush x:Key="DimBrush" Color="#78838C"/>

        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CyanMonoStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource CyanBrush}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="MetricLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="MetricValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource CyanBrush}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>

        <Style x:Key="ThinProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Background" Value="#111820"/>
            <Setter Property="Foreground" Value="{StaticResource CyanBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3"
                                    HorizontalAlignment="Left"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusPillStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="BorderThickness" Value="1.4"/>
            <Setter Property="Padding" Value="13,8"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360"/>
            <ColumnDefinition Width="26"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="26,26,0,26">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="26"/>
                <RowDefinition Height="330"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="66"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            BorderBrush="{StaticResource LineBrush}"
                            BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal" Margin="20,0" VerticalAlignment="Center">
                            <Path Data="M25,12 A13,13 0 1 0 25,38 A13,13 0 1 0 25,12 M12,25 L5,25 M8,18 L5,25 L12,32"
                                  Stroke="{StaticResource MutedBrush}"
                                  StrokeThickness="3"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Width="24"
                                  Height="24"
                                  Stretch="Uniform"/>
                            <TextBlock Text="{root:Loc 最近检测条码}"
                                       Style="{StaticResource SectionTitleStyle}"
                                       Margin="12,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="20,20,20,20">
                            <Border Height="56" Background="#202A35" CornerRadius="2" Padding="14,0">
                                <Grid>
                                    <TextBlock Text="SN-99482-XA1" Style="{StaticResource CyanMonoStyle}"/>
                                    <TextBlock Text="OK" Foreground="{StaticResource GreenBrush}" FontFamily="Consolas" FontSize="19" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Border Height="56" BorderBrush="#2B3744" BorderThickness="0,0,0,1" Padding="14,0">
                                <Grid>
                                    <TextBlock Text="SN-99481-XA1" Style="{StaticResource CyanMonoStyle}"/>
                                    <TextBlock Text="OK" Foreground="{StaticResource GreenBrush}" FontFamily="Consolas" FontSize="19" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Border Height="68" Background="#3A2028" Padding="14,0">
                                <Grid>
                                    <TextBlock Text="SN-99480-XA1" Style="{StaticResource CyanMonoStyle}"/>
                                    <TextBlock Text="NG" Foreground="{StaticResource RedBrush}" FontFamily="Consolas" FontSize="19" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock.Effect>
                                            <DropShadowEffect Color="#FF5258" BlurRadius="12" ShadowDepth="0" Opacity="0.85"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                </Grid>
                            </Border>
                            <Border Height="56" BorderBrush="#2B3744" BorderThickness="0,0,0,1" Padding="14,0">
                                <Grid>
                                    <TextBlock Text="SN-99479-XA1" Style="{StaticResource CyanMonoStyle}"/>
                                    <TextBlock Text="OK" Foreground="{StaticResource GreenBrush}" FontFamily="Consolas" FontSize="19" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Border Height="56" Padding="14,0">
                                <Grid>
                                    <TextBlock Text="SN-99478-XA1" Style="{StaticResource CyanMonoStyle}"/>
                                    <TextBlock Text="OK" Foreground="{StaticResource GreenBrush}" FontFamily="Consolas" FontSize="19" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource PanelBorderStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="80"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1">
                        <Grid Margin="20,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Path Data="M24,5 L45,42 L3,42 Z M24,17 L24,28 M24,35 L24,36"
                                      Stroke="#C4CFD8"
                                      StrokeThickness="3"
                                      StrokeLineJoin="Round"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      Width="24"
                                      Height="24"
                                      Stretch="Uniform"/>
                                <TextBlock Text="{root:Loc 系统警报}" Style="{StaticResource SectionTitleStyle}" Margin="12,0,0,0"/>
                            </StackPanel>
                            <Border Background="#7D3C45" CornerRadius="3" Padding="11,6" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="{root:Loc 1 未处理}" Foreground="{StaticResource RedBrush}" FontSize="14"/>
                            </Border>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="1" Margin="20,24,20,0">
                        <Grid Margin="0,0,0,28">
                            <Border Width="3" Background="{StaticResource RedBrush}" HorizontalAlignment="Left"/>
                            <StackPanel Margin="22,0,0,0">
                                <TextBlock Text="{root:Loc A4: 噪音超标}" Foreground="{StaticResource RedBrush}" FontSize="18" FontWeight="Bold"/>
                                <TextBlock Text="{root:Loc SN-99480-XA1 测量值 78.9dB (限值 65.0)}"
                                           Foreground="#BFC8D1"
                                           FontSize="15"
                                           Margin="0,12,0,0"/>
                                <TextBlock Text="14:21:10 UTC" Foreground="#7F8B95" FontSize="13" Margin="0,14,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <Border Width="3" Background="#D59A00" HorizontalAlignment="Left"/>
                            <StackPanel Margin="22,0,0,0">
                                <TextBlock Text="{root:Loc Cam-2 离线}" Foreground="#D59A00" FontSize="18" FontWeight="Bold"/>
                                <TextBlock Text="{root:Loc B线视图连接丢失}" Foreground="#BFC8D1" FontSize="15" Margin="0,12,0,0"/>
                                <TextBlock Text="13:45:00 UTC" Foreground="#7F8B95" FontSize="13" Margin="0,14,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Column="2" Margin="0,26,26,26">
            <Grid.RowDefinitions>
                <RowDefinition Height="330"/>
                <RowDefinition Height="26"/>
                <RowDefinition Height="54"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 视频监控占位区 -->
            <Border Grid.Row="0" Style="{StaticResource PanelBorderStyle}" ClipToBounds="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="82"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#2D3540" Offset="0"/>
                                <GradientStop Color="#59616A" Offset="0.38"/>
                                <GradientStop Color="#242D36" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Grid Margin="20,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Rectangle Width="14" Height="14" Stroke="{StaticResource CyanBrush}" StrokeThickness="2" Margin="0,0,12,0"/>
                                <TextBlock Text="{root:Loc 海康威视 01号摄像头 - 主测试工位}"
                                           Foreground="#D2D9DF"
                                           FontSize="17"
                                           VerticalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="▮▮▮ · 1080P 60FPS"
                                           Foreground="{StaticResource CyanBrush}"
                                           FontFamily="Consolas"
                                           FontSize="17"
                                           Margin="0,0,22,0"/>
                                <Border BorderBrush="#009E6B" BorderThickness="1.3" CornerRadius="2" Padding="17,9" Background="#1E5148">
                                    <TextBlock Text="{root:Loc 实时}" Foreground="{StaticResource GreenBrush}" FontSize="17" FontWeight="Bold"/>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.Background>
                            <ImageBrush ImageSource="pack://application:,,,/Images/mock_video.jpg" Stretch="UniformToFill"/>
                        </Grid.Background>

                        <!-- 半透明深蓝色遮罩 -->
                        <Border Background="#33001A33"/>

                        <!-- 悬浮UI层 - 四个角落的折角边框 -->
                        <!-- 左上折角 -->
                        <Path Data="M 0,16 L 0,0 L 16,0" Stroke="{StaticResource CyanBrush}" StrokeThickness="2.5" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="16"/>
                        <!-- 右上折角 -->
                        <Path Data="M 16,16 L 16,0 L 0,0" Stroke="{StaticResource CyanBrush}" StrokeThickness="2.5" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="16"/>
                        <!-- 左下折角 -->
                        <Path Data="M 0,0 L 0,16 L 16,16" Stroke="{StaticResource CyanBrush}" StrokeThickness="2.5" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="16"/>
                        <!-- 右下折角 -->
                        <Path Data="M 16,0 L 16,16 L 0,16" Stroke="{StaticResource CyanBrush}" StrokeThickness="2.5" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="16"/>

                        <!-- 左上角摄像头名称 -->
                        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="32" Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource GreenBrush}" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.2" Duration="0:0:0.8" AutoReverse="True"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{root:Loc CAM-01 主测试工位}" Foreground="White" FontSize="13" FontWeight="Bold">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="Black" Opacity="0.8"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>

                        <!-- 右上角绿色"实时"按钮和参数 -->
                        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="32" Orientation="Horizontal">
                            <Border BorderBrush="{StaticResource GreenBrush}" BorderThickness="1" CornerRadius="2" Padding="6,2" Background="#66004D33" Margin="0,0,10,0">
                                <TextBlock Text="LIVE" Foreground="{StaticResource GreenBrush}" FontSize="11" FontWeight="Bold"/>
                            </Border>
                            <TextBlock Text="1080P @ 60FPS | 4.8 Mbps" Foreground="#E2E8F0" FontSize="12" FontFamily="Consolas" VerticalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="Black" Opacity="0.8"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>

                        <!-- 中心准星 -->
                        <Grid Width="60" Height="60" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.85">
                            <Ellipse Stroke="{StaticResource CyanBrush}" StrokeThickness="1" StrokeDashArray="3 2"/>
                            <Line X1="30" Y1="0" X2="30" Y2="12" Stroke="{StaticResource CyanBrush}" StrokeThickness="1"/>
                            <Line X1="30" Y1="48" X2="30" Y2="60" Stroke="{StaticResource CyanBrush}" StrokeThickness="1"/>
                            <Line X1="0" Y1="30" X2="12" Y2="30" Stroke="{StaticResource CyanBrush}" StrokeThickness="1"/>
                            <Line X1="48" Y1="30" X2="60" Y2="30" Stroke="{StaticResource CyanBrush}" StrokeThickness="1"/>
                            <Ellipse Width="4" Height="4" Fill="{StaticResource CyanBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Grid>
            </Border>

            <!-- 阶段标题 -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="26"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="26"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Background="#242C36">
                    <TextBlock Text="{root:Loc ● 阶段 1: 空载测试}" Foreground="{StaticResource CyanBrush}" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource PanelBorderStyle}" Background="#242C36">
                    <TextBlock Text="{root:Loc ● 阶段 2: 噪音测试}" Foreground="{StaticResource CyanBrush}" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Column="4" Style="{StaticResource PanelBorderStyle}" Background="#242C36">
                    <TextBlock Text="{root:Loc ● 阶段 3: 负载测试}" Foreground="{StaticResource CyanBrush}" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- 2x3 工位矩阵布局 -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="26"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="26"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <!-- A1 (第一行, 阶段 1) -->
                <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Background="#243040" Padding="20">
                    <Grid>
                        <Border Style="{StaticResource StatusPillStyle}" BorderBrush="{StaticResource GreenBrush}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="9" Height="9" Fill="{StaticResource GreenBrush}" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{root:Loc 运行中}" Foreground="{StaticResource GreenBrush}" FontSize="15" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="A1: FX5U" Foreground="#D9E1EA" FontSize="27" FontWeight="Bold"/>
                            <TextBlock Text="{Binding NoLoadStations[0].Barcode}" Foreground="{StaticResource CyanBrush}" FontFamily="Consolas" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" HorizontalAlignment="Left" Margin="0,12,0,14"/>
                            <Border Height="1" Background="#2F3A47" Margin="0,0,0,18"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{root:Loc 电压}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="220.1" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" V" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="14,0,0,0">
                                    <TextBlock Text="{root:Loc 电流}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoLoadStations[0].NoLoadCurrent, StringFormat={}{0:F1}}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" A" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="14,0,0,0">
                                    <TextBlock Text="{root:Loc 转速}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoLoadStations[0].NoLoadSpeed}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" RPM" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="#2F3A47" Margin="0,16,0,26"/>
                            <Grid>
                                <ProgressBar Value="60" Style="{StaticResource ThinProgressBarStyle}" VerticalAlignment="Center"/>
                                <TextBlock Text="60%" Foreground="{StaticResource CyanBrush}" FontFamily="Consolas" FontSize="17" HorizontalAlignment="Right" Margin="0,0,0,-26"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- A3 (第一行, 阶段 2) -->
                <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource PanelBorderStyle}" Background="#243040" Padding="20">
                    <Grid>
                        <Border Style="{StaticResource StatusPillStyle}" BorderBrush="{StaticResource GreenBrush}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="9" Height="9" Fill="{StaticResource GreenBrush}" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{root:Loc 运行中}" Foreground="{StaticResource GreenBrush}" FontSize="15" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="A3: NV-X" Foreground="#D9E1EA" FontSize="27" FontWeight="Bold"/>
                            <TextBlock Text="{Binding NoiseStations[0].Barcode}" Foreground="{StaticResource CyanBrush}" FontFamily="Consolas" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" HorizontalAlignment="Left" Margin="0,12,0,14"/>
                            <Border Height="1" Background="#2F3A47" Margin="0,0,0,18"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{root:Loc 噪音分贝}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoiseStations[0].FwdNoise, StringFormat={}{0:F1}}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" dB" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="14,0,0,0">
                                    <TextBlock Text="{root:Loc 振动值}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoiseStations[0].RevNoise, StringFormat={}{0:F1}}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" mm/s" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="14,0,0,0">
                                    <TextBlock Text="{root:Loc 温度}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoiseStations[0].NoiseDiff, StringFormat={}{0:F1}}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" °C" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="#2F3A47" Margin="0,16,0,26"/>
                            <Grid>
                                <ProgressBar Value="85" Style="{StaticResource ThinProgressBarStyle}" VerticalAlignment="Center"/>
                                <TextBlock Text="85%" Foreground="{StaticResource CyanBrush}" FontFamily="Consolas" FontSize="17" HorizontalAlignment="Right" Margin="0,0,0,-26"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- A5 (第一行, 阶段 3) -->
                <Border Grid.Row="0" Grid.Column="4" Style="{StaticResource PanelBorderStyle}" Background="#243040" BorderBrush="#00A66C" Padding="20">
                    <Grid>
                        <Border Style="{StaticResource StatusPillStyle}" BorderBrush="{StaticResource GreenBrush}">
                            <TextBlock Text="{root:Loc 已完成}" Foreground="{StaticResource GreenBrush}" FontSize="15" FontWeight="Bold"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="A5: LD-Max" Foreground="#D9E1EA" FontSize="27" FontWeight="Bold"/>
                            <TextBlock Text="{Binding LoadStations[0].Barcode}" Foreground="{StaticResource MutedBrush}" FontFamily="Consolas" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" HorizontalAlignment="Left" Margin="0,12,0,14"/>
                            <Border Height="1" Background="#2F3A47" Margin="0,0,0,18"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{root:Loc 最大扭矩}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding LoadStations[0].LoadCurrent, StringFormat={}{0:F1}}" Foreground="#D8DFE7" FontFamily="Consolas" FontSize="28"/>
                                        <TextBlock Text=" Nm" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="18,0,0,0">
                                    <TextBlock Text="{root:Loc 最高温度}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding LoadStations[0].LoadSpeed, StringFormat={}{0:F1}}" Foreground="#D8DFE7" FontFamily="Consolas" FontSize="28"/>
                                        <TextBlock Text=" °C" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="#2F3A47" Margin="0,18,0,18"/>
                            <Border Background="#174840" Height="40" CornerRadius="2">
                                <TextBlock Text="{root:Loc 扭矩: OK - 合格}" Foreground="{StaticResource GreenBrush}" FontFamily="Consolas" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- A2 (第二行, 阶段 1) -->
                <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Background="#11171D" BorderBrush="#222C36" Padding="20" Opacity="0.82">
                    <Grid>
                        <Border Background="#232A32" CornerRadius="2" Padding="13,8" HorizontalAlignment="Right" VerticalAlignment="Top">
                            <TextBlock Text="{root:Loc 待机}" Foreground="#9AA4AE" FontSize="15"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="A2: FX5U" Foreground="#9EA8B2" FontSize="27" FontWeight="Bold"/>
                            <TextBlock Text="{root:Loc 等待中}" Foreground="#9EA8B2" FontSize="18" Margin="0,12,0,14"/>
                            <Border Height="1" Background="#26313A" Margin="0,0,0,18"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{root:Loc 电压}" Style="{StaticResource MetricLabelStyle}"/>
                                    <TextBlock Text="-- V" Foreground="#8B969F" FontFamily="Consolas" FontSize="22"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="14,0,0,0">
                                    <TextBlock Text="{root:Loc 电流}" Style="{StaticResource MetricLabelStyle}"/>
                                    <TextBlock Text="-- A" Foreground="#8B969F" FontFamily="Consolas" FontSize="22"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="14,0,0,0">
                                    <TextBlock Text="{root:Loc 转速}" Style="{StaticResource MetricLabelStyle}"/>
                                    <TextBlock Text="-- RPM" Foreground="#8B969F" FontFamily="Consolas" FontSize="22"/>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="#26313A" Margin="0,18,0,26"/>
                            <Grid>
                                <ProgressBar Value="0" Style="{StaticResource ThinProgressBarStyle}" Foreground="#2A323B" VerticalAlignment="Center"/>
                                <TextBlock Text="{root:Loc 准备就绪}" Foreground="#9EA8B2" FontSize="15" HorizontalAlignment="Right" Margin="0,0,0,-26"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- A4 (第二行, 阶段 2) -->
                <Border Grid.Row="2" Grid.Column="2" Background="#1F0E10" BorderBrush="{StaticResource RedBrush}" BorderThickness="2.2" CornerRadius="4" Padding="20">
                    <Grid>
                        <Border Style="{StaticResource StatusPillStyle}" BorderBrush="{StaticResource RedBrush}">
                            <TextBlock Text="{root:Loc ⚠ 故障}" Foreground="{StaticResource RedBrush}" FontSize="16" FontWeight="Bold"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="A4: NV-X" Foreground="#D9E1EA" FontSize="27" FontWeight="Bold"/>
                            <TextBlock Text="{Binding NoiseStations[1].Barcode}" Foreground="{StaticResource RedBrush}" FontFamily="Consolas" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" HorizontalAlignment="Left" Margin="0,12,0,14"/>
                            <Border Height="1" Background="#743035" Margin="0,0,0,18"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{root:Loc 噪音分贝}" Foreground="#C1A0A2" FontSize="14" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoiseStations[1].FwdNoise, StringFormat={}{0:F1}}" Foreground="{StaticResource RedBrush}" FontFamily="Consolas" FontSize="28" FontWeight="Bold"/>
                                        <TextBlock Text=" dB" Foreground="{StaticResource RedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                    <TextBlock Text="{root:Loc 限值}" Foreground="#C1A0A2" FontSize="14" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding NoiseStations[1].NoiseDiff, StringFormat={}{0:F1}}" Foreground="#D8DFE7" FontFamily="Consolas" FontSize="28"/>
                                        <TextBlock Text=" dB" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="#743035" Margin="0,18,0,18"/>
                            <Border Background="#431819" Height="40" CornerRadius="2">
                                <TextBlock Text="{root:Loc 检测结果: NG - 需人工检视}" Foreground="{StaticResource RedBrush}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- A6 (第二行, 阶段 3) -->
                <Border Grid.Row="2" Grid.Column="4" Style="{StaticResource PanelBorderStyle}" Background="#243040" Padding="20">
                    <Grid>
                        <Border Style="{StaticResource StatusPillStyle}" BorderBrush="{StaticResource GreenBrush}">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="9" Height="9" Fill="{StaticResource GreenBrush}" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{root:Loc 运行中}" Foreground="{StaticResource GreenBrush}" FontSize="15" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="A6: LD-Max" Foreground="#D9E1EA" FontSize="27" FontWeight="Bold"/>
                            <TextBlock Text="{Binding LoadStations[1].Barcode}" Foreground="{StaticResource CyanBrush}" FontFamily="Consolas" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxWidth="240" HorizontalAlignment="Left" Margin="0,12,0,14"/>
                            <Border Height="1" Background="#2F3A47" Margin="0,0,0,18"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="1*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{root:Loc 扭矩}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding LoadStations[1].LoadCurrent, StringFormat={}{0:F1}}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" Nm" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="18,0,0,0">
                                    <TextBlock Text="{root:Loc 温度}" Style="{StaticResource MetricLabelStyle}"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding LoadStations[1].LoadSpeed, StringFormat={}{0:F1}}" Style="{StaticResource MetricValueStyle}"/>
                                        <TextBlock Text=" °C" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="#2F3A47" Margin="0,18,0,26"/>
                            <Grid>
                                <ProgressBar Value="20" Style="{StaticResource ThinProgressBarStyle}" VerticalAlignment="Center"/>
                                <TextBlock Text="20%" Foreground="{StaticResource CyanBrush}" FontFamily="Consolas" FontSize="17" HorizontalAlignment="Right" Margin="0,0,0,-26"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
