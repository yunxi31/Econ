# 2026-06-11 工作日志

## 西门子S7协议通信实现
- 添加 S7netPlus 0.20.0 NuGet 包
- 创建 `Services/S7PlcClient.cs`，实现 `IPlcClient` 接口
  - 使用 S7netPlus 库与 S7-1200/S7-1500 通信
  - PLC 地址映射：完成信号 M100.0，测试数据 DB1.DBW100 起，条码 DB1.DBD200
  - CpuType 根据配置自动解析（S7-1200/S7-1500/S7-300/S7-400/S7-200SMART）
  - Plc 构造函数签名：`Plc(CpuType, ip, port, short rack, short slot)`
  - S7netPlus ReadAsync 返回 object?，需安全拆箱
- 更新 `PlcClientFactory.cs`，注册 S7 协议分支：
  - "S7 Protocol (TCP)" / "S7 Protocol / TCP" / "S7" → S7PlcClient
- 编译通过，0 错误

## SQLite + SqlSugar 数据层重构
- 添加 SqlSugarCore 5.1.4.214 NuGet 包
- 创建数据库实体类（`Models/Entities/` 目录）：
  - `MotorTestRecordEntity` — 电机测试记录（自增主键 + 条码业务键）
  - `UserEntity` — 用户（枚举存为 int：Role/Status）
  - `StationConfigEntity` — 工位配置
  - `NotificationEntity` — 通知记录
- 创建 `SqlSugarDbContext`：管理 SQLite 连接 + CodeFirst 建表 + 种子数据
  - 数据库路径：`AppDir/Data/MotorTest.db`
  - 自动建表（CodeFirst.InitTables）
  - 种子数据仅在表为空时插入（用户 16 个 + 工位 6 个）
- 创建 `SqlMotorTestRepository`：实现 `IMotorTestRepository`
  - UpsertStageResultAsync → 查重 + 插入/更新
  - Query/GetRecent/GetSummary/GetDefect/GetFaultRanking → 全部基于 SqlSugar LINQ
  - Entity → Model 转换方法 ToModel()
- 创建 `SqlSugarUserService`：实现 `IUserService`
  - 枚举用 int 存储，查询时转换
  - ID 自增序列从数据库已有记录推导
  - HashPassword 复用 InMemoryUserService 的 SHA256 实现
- 更新 `BackendRuntime`：
  - 构造函数新增 `SqlSugarDbContext DbContext` 参数
  - CreateDefault() 改为使用 SqlSugar 系列服务
  - 工位配置从数据库加载（而非硬编码）
  - 测试数据种子仅在首次运行（表为空）时插入
- 编译通过，0 错误
