## Daily Log - 2026-06-15

### ViewModel/View 绑定与通信架构审查
- 完成全面的架构审查，报告：docs/绑定与通信审查报告.md
- **严重违规**：HistoryViewModel 直接引用 View 命名空间 + new PrintDialog() + new MotorReportWindow() + Clipboard；UserViewModel 直接引用 View 命名空间 + new UserEditWindow()
- **高风险**：DashboardViewModel.DefectItem.ColorBrush 数据模型混入 System.Windows.Media.BrushConverter
- **中风险**：6 个 ViewModel 直接调用 MessageBox.Show()；ExportCsv 中直接使用 SaveFileDialog
- **通信评估**：C# 事件 + Channel 方案足够，但缺少事件聚合器/弱事件模式；_snapshotChannel 闲置未消费
- **DI 评估**：手动 Service Locator 模式（无容器），单例配置基本合理，PlcClientFactory 应瞬态
- **推荐 P0 修复**：创建 IDialogService 接口隔离对话框/窗口/打印

### C# 性能优化审查 — 硬件通讯层
- 完成对 PlcPollingService / ModbusTcpClient / MelsecMcClient / S7PlcClient / BackendRuntime 及 3 个 ViewModel 的性能审查
- **发现 7 个问题**：Dashboard UI 线程劫持（严重）、启动同步阻塞嵌套（严重）、无缓冲机制（中）、Dictionary 线程不安全（中）、S7 三次独立 TCP 读取（轻）、SemaphoreSlim 持锁过长（轻）
- **核心矛盾**：高速数据采集(500ms)与低速 SQLite 写入混合在同一同步循环中
- **推荐重构优先级**：A1(UI线程隔离+节流) > A3(异步工厂) > B1(Channel<T>写缓冲区) > A2(ConcurrentDictionary)
