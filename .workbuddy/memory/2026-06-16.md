# 2026-06-16

## Bug 修复 — XAML 绑定与 ICommand 缺陷修复 (tasks2.md)

### 执行范围
根据 tasks2.md 和 design2.md 中的 5 个 Bug 修复计划，完成了全部修复任务。

### Bug 1: DashboardView 图表增量更新
- **状态**: 已有代码修复（ViewModel 已使用 ObservableCollection<ISeries> + 增量更新）
- **测试**: DashboardPerformanceTests 全部通过

### Bug 2: ConfigViewModel TestConnectionAsync 并发保护
- **状态**: 已有代码修复（IsTestingConnection + CanTestConnection + [RelayCommand(CanExecute = ...)]）
- **测试**: ConfigViewModelTests 全部通过

### Bug 3: HistoryViewModel PrintTraceAsync 并发保护
- **状态**: 已有代码修复（IsPrinting + CanPrintTrace + [RelayCommand(CanExecute = ...)]）
- **测试**: HistoryViewModelTests 全部通过

### Bug 4: MonitorView 硬编码数据绑定化
- **状态**: ✅ 本次修复完成
- **View/Model 侧**（已有代码）:
  - StationState 已添加 Progress/Voltage/Current/Rpm 属性
  - MonitorViewModel 已添加 RecentBarcodes/Alerts 集合
  - ApplySnapshot 已更新上述属性
- **XAML 侧**（本次修改）:
  - ProgressBar Value="60/85/0/20" → `{Binding station.Progress}`
  - ProgressBar 百分比文本 "60%/85%/20%" → `{Binding station.Progress, StringFormat={}{0:F0}%}`
  - A1 电压硬编码 "220.1" → `{Binding NoLoadStations[0].Voltage}`
  - A2 参数 "-- V/-- A/-- RPM" → `{Binding NoLoadStations[1].Voltage/Current/Rpm}`
  - 条码列表 5 个硬编码 → `ItemsControl ItemsSource="{Binding RecentBarcodes}"`
  - 警报列表 2 个硬编码 → `ItemsControl ItemsSource="{Binding Alerts}"`
- **测试**: MonitorViewModelTests 全部通过（含 Bug Condition + Preservation）

### Bug 5: HistoryViewModel 分页按钮 CanExecute
- **状态**: 已有代码修复（CanPreviousPage + CanNextPage + [RelayCommand(CanExecute = ...)]）
- **测试**: HistoryViewModelTests 全部通过

### 测试修复
- MonitorViewModelTests 改为直接调用 ApplySnapshot 反射方法（绕过测试环境中 Application.Current 静态字段被并发重置导致 RunOnUiThread 失效的问题）

### 最终结果
- **全部 16/16 测试通过**（含预存在的 UiThreadFlooding 测试）
- 0 个构建错误
