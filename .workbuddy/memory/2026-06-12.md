## 2026-06-12 通知服务接入数据库

### 完成工作
1. **创建 SqlSugarNotificationService** - 基于 SQLite + SqlSugar 的通知服务实现
   - 构造时从数据库加载所有通知到内存 ObservableCollection
   - 每次操作（Add/AddRange/MarkAsRead/MarkAllAsRead/Remove/ClearAll）同步写入数据库
   - 保持内存和数据库的双向同步
   - Entity ↔ Model 转换

2. **更新 SqlSugarDbContext** - 添加通知种子数据（12条：3报警 + 4维护 + 5系统）

3. **更新 BackendRuntime** - `CreateDefault()` 使用 `SqlSugarNotificationService` 替代 `InMemoryNotificationService`

4. **更新测试项目 MotorTestDbTest**
   - 添加通知种子数据函数 SeedNotifications
   - 种子数据验证：通知数=12，类型/严重程度分布正确
   - 新增 20 个通知 CRUD 测试：插入、标记已读、批量已读、删除、清空、批量插入、按类型/来源/时间查询、跨实例持久化、枚举映射一致性
   - 全部 146 个测试通过（含原有126个 + 新增20个）

### 涉及的数据库表
- Notifications 表已存在（NotificationEntity），现在有种子数据

### 修复 Bug (2026-06-12 下午)
1. **数据追溯 Stack overflow** — ① HistoryViewModel 构造函数中的 `SearchCommand.Execute(null)` 在 XAML 模板加载时产生循环实例化。修复：移除构造函数中的查询触发，改为 `EnsureInitialized()` 懒加载模式。② 移除 HistoryView.xaml 中 DatePicker/Calendar/CalendarItem 的自定义 ControlTemplate（DarkCalendarDayButtonStyle / DarkCalendarButtonStyle / DarkCalendarItemStyle / DarkCalendarStyle / DarkDatePickerStyle），保留简化版 DarkDatePickerStyle（仅外框样式，日历弹出使用默认 WPF Calendar 模板）。根因是 CalendarItem 的 `ComponentResourceKey` DataTemplate + `DataTrigger` + 复杂模板在 BAML 加载时产生递归实例化。
2. **通知图标→日志中心** — 当 CurrentView 已经是 NotificationVM 实例时，`CurrentView = NotificationVM` 不触发 PropertyChanged（引用没变）。修复：先设 `null` 再设回，强制界面刷新。
